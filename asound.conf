#
# Bosco Core - ALSA Configuration
# Minimal config that works without errors
#

# Use PulseAudio by default
pcm.!default {
    type pulse
}

ctl.!default {
    type pulse
}

# Null device for fallback
pcm.null {
    type null
}

ctl.null {
    type null
}

